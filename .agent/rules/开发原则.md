---
trigger: always_on
---

1. 因为这是一个ai数据站，记录价格变化。积累的数据越多，积累的价格变化越多。会越有意义。所以在任何时候，保持数据跟官网数据的100%相同是开发的第一原则，也是最重要的。
2. 因为官方更新模型趋向于将重要的模型靠前放，所以我希望当前项目的模型数据跟官网保持一致。
3. 开发过程中，生成的测试data下的测试数据，不能保留，要及时撤销掉。data的数据唯一获取方式就是git pull拉取到，除非是本地修正错误数据。
4. raw下的数据为原始抓取的网页数据，禁止进行任何修改操作。
5. 抓取到的模型id必须跟官网的模型保持一致。这是后续一切数据比对的基础。