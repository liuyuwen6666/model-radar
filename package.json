{
  "name": "ai-model-price-radar",
  "version": "1.0.0",
  "description": "Static AI model pricing radar with JSON data updates and lightweight Node.js scripts.",
  "main": "index.js",
  "scripts": {
    "build": "node scripts/prepare-static.js && npx tailwindcss -i ./src/input.css -o ./public/dist/styles.css --minify",
    "deploy": "wrangler deploy",
    "schema:check": "node scripts/check-structured-data.js",
    "update": "node scripts/update.js",
    "diff": "node scripts/diff.js",
    "save-raw": "node scripts/save-raw.js"
  },
  "keywords": [
    "ai",
    "llm",
    "pricing",
    "radar"
  ],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19",
    "wrangler": "^4.47.0"
  }
}
